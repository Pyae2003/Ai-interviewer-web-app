import { RequestPasswordResetForm } from "@/features/clients/auth/components/reset-password-form"

const page = () => {
  return (
    <div>
        <RequestPasswordResetForm/>
    </div>
  )
}

export default page